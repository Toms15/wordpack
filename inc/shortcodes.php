<?php
/**
 * Sample implementation of the Shortcodes
 *
 * @package WordPack
 */

// add_shortcode('highlighted', function ($atts, $content = null) {
//     extract(shortcode_atts([
//         'color' => false,
//     ], $atts));
//     $classes = implode(' ', ['highlighted', $color ? 'tape--' . $color : '' ]);
//     return sprintf('<span class="%s">%s</span>', $classes, $content);
// });